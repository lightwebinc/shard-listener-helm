{{- if eq .Values.workloadType "Deployment" }}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "shard-listener.fullname" . }}
  labels:
    {{- include "shard-listener.labels" . | nindent 4 }}
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      {{- include "shard-listener.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      annotations:
        {{- if eq .Values.networking.mode "multus" }}
        {{- include "shard-listener.multusAnnotation" . | nindent 8 }}
        {{- end }}
        {{- with .Values.podAnnotations }}
        {{- toYaml . | nindent 8 }}
        {{- end }}
      labels:
        {{- include "shard-listener.selectorLabels" . | nindent 8 }}
        {{- with .Values.podLabels }}
        {{- toYaml . | nindent 8 }}
        {{- end }}
    spec:
      {{- include "shard-listener.podSpec" . | nindent 6 }}
{{- end }}
